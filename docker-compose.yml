version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: topupgame_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: topup_admin
      POSTGRES_PASSWORD: TopUpDb#2026
      POSTGRES_DB: topupgame_db
      ADMIN_PASSWORD: CTF{4dm1n_P4ssw0rd_B0c0r}
      CTF_FLAG: CTF{A8a_lu_liat_5ql_1nject}
    ports:
      - "5433:5432"    # host:container -> akses dari luar container pakai 5433
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./db/z-init-flags.sh:/docker-entrypoint-initdb.d/z-init-flags.sh
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U topup_admin -d topupgame_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: topupgame_db
      DB_USER: topup_admin
      DB_PASSWORD: TopUpDb#2026
      JWT_SECRET: CTF{JwT_S3cr3t_K3np_B0c0r}
      API_KEY: CTF{4p1_K3y_B0c0r_w0i}
    ports:
      - "3000:3000"
volumes:
  pgdata: