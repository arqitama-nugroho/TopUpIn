version: '3.8'

services:
  db:
    image: postgres:16-alpine
    container_name: topupgame_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: topup_admin
      POSTGRES_PASSWORD: TopUpDb#2026
      POSTGRES_DB: topupgame_db
      # Nilai flag tersimpan terenkode (base64) — didekode saat init DB
      ADMIN_PASSWORD: Q1RGezRkbTFuX1A0c3N3MHJkX0IwYzByfQ==
      CTF_FLAG: Q1RGe0E4YV9sdV9saWF0XzVxbF8xbmplY3R9
    ports:
      - "5433:5432"    # host:container -> akses dari luar container pakai 5433
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./db/z-init-flags.sh:/docker-entrypoint-initdb.d/z-init-flags.sh
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U topup_admin -d topupgame_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_HOST: db
      DB_PORT: 5432
      DB_NAME: topupgame_db
      DB_USER: topup_admin
      DB_PASSWORD: TopUpDb#2026
      # Nilai flag tersimpan terenkode (base64) — didekode di app.js
      JWT_SECRET: Q1RGe0p3VF9TM2NyM3RfSzNucF9CMGMwcn0=
      API_KEY: Q1RGezRwMV9LM3lfQjBjMHJfdzBpfQ==
    ports:
      - "3000:3000"
volumes:
  pgdata: